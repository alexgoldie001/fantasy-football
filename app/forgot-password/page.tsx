'use client';
import { FormEvent, useState } from 'react';
import { supabaseBrowser } from '@/lib/supabase-browser';
import Link from 'next/link';
export default function ForgotPasswordPage(){const [email,setEmail]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const {error}=await supabaseBrowser().auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setLoading(false);setMessage(error?'We could not send a reset email. Please check the address and try again.':'If that email belongs to a manager, a reset link is on its way.');}return <main className="public-form-page"><section className="public-form"><p className="eyebrow">Account recovery</p><h1>Reset your password.</h1><p className="helper">Enter your manager email and we’ll send a secure reset link.</p><form onSubmit={submit}><label className="field">Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@email.com"/></label><button className="primary" disabled={loading}>{loading?'Sending…':'Send reset email'}</button></form>{message&&<p className="success">{message}</p>}<Link href="/" className="forgot-link">Back to sign in</Link></section></main>}
