'use client';
import { AppShell } from '@/components/app-shell'; import { useEffect,useState } from 'react'; import { supabaseBrowser } from '@/lib/supabase-browser';
export default function CupPage(){const [data,setData]=useState<any>(null),[active,setActive]=useState(0),[admin,setAdmin]=useState(false),[message,setMessage]=useState('');const load=()=>fetch('/api/cups',{cache:'no-store'}).then(r=>r.json()).then(setData);useEffect(()=>{load();supabaseBrowser().auth.getSession().then(async({data:{session}})=>{if(!session)return;const r=await fetch('/api/me',{headers:{Authorization:`Bearer ${session.access_token}`}});const x=await r.json();setAdmin(Boolean(x.user?.isAdmin));});},[]);async function changeDate(id:string,date:string){const {data:{session}}=await supabaseBrowser().auth.getSession();const r=await fetch('/api/cups',{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session?.access_token||''}`},body:JSON.stringify({fixtureId:id,date})});const x=await r.json();setMessage(r.ok?'Fixture date saved.':x.error);if(r.ok)load();}const divisions=data?.divisions||[],d=divisions[active];return <AppShell><section className="page-heading"><p className="eyebrow">Head-to-head competition</p><h1>Cup.</h1></section>{!data?<p className="helper">Loading cups…</p>:!data.cup?<section className="panel"><h2>COMING SOON</h2><p className="sub">The commissioner has not generated the mini leagues yet.</p></section>:<><div className="score-links">{divisions.map((x:any,i:number)=><button className="table-link" key={x.id} onClick={()=>setActive(i)}>{x.name}</button>)}</div><section className="panel period-scores"><p className="eyebrow">{d.name}</p><h2>Mini-league table</h2><div className="period-table-head"><span>#</span><span>Team</span><span>P</span><span>W</span><span>D</span><span>GD</span><span>Pts</span></div>{d.table.map((row:any,i:number)=><div className="period-score-row" key={row.squadId}><b>{i+1}</b><div><strong>{row.team}</strong><small>{row.manager}</small></div><span>{row.played}</span><span>{row.wins}</span><span>{row.draws}</span><span>{row.for-row.against}</span><strong>{row.points}</strong></div>)}{d.playoff&&<p className="helper">Top two qualify for a play-off to decide the winner.</p>}<h2>Fixtures & results</h2>{d.fixtures.sort((a:any,b:any)=>a.round_number-b.round_number).map((f:any)=>{const home=d.entries.find((e:any)=>e.squad_id===f.home_squad_id),away=d.entries.find((e:any)=>e.squad_id===f.away_squad_id);return <div className="cup-fixture" key={f.id}><span>Round {f.round_number}</span><strong>{home?.team} {f.score.home} – {f.score.away} {away?.team}</strong>{admin?<input type="date" value={f.starts_at.slice(0,10)} onChange={e=>changeDate(f.id,e.target.value)}/>:<small>{new Date(f.starts_at).toLocaleDateString('en-GB',{day:'numeric',month:'short'})}</small>}</div>})}</section>{message&&<p className="success">{message}</p>}</>}</AppShell>}
